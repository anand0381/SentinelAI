import EmptyState from '../components/ui/EmptyState.jsx';

function SettingsPage() {
  return (
    <EmptyState
      title="Settings"
      description="Workspace preferences and application configuration will be managed here in a later sprint."
    />
  );
}

export default SettingsPage;
